{
  "name": "whisker-server-release",
  "private": true,
  "description": "Release tooling only — the server itself is Go (go.mod)",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.2.9"
  }
}
